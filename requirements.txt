# 钉钉群报修工单归档系统 v4.0 — Python 依赖
# 计划书 Task 4

# ─── HTTP 客户端（模型调用） ───
httpx>=0.27,<1.0

# ─── 协议校验 ───
jsonschema>=4.0,<5.0

# ─── 淘宝对账表导入（读取订单地址明细.xlsx） ───
openpyxl>=3.1,<4.0

# ─── 测试 ───
pytest>=8.0,<9.0
pytest-asyncio>=0.24,<1.0
