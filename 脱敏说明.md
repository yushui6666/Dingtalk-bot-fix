# 副本脱敏说明

本目录是「钉钉报修工单归档系统」的**脱敏副本**，用于对外分发/交接给其他 Agent 或协作者。

## 一、包含范围

- 仅 `dingtalk_script` 仓库的**源码、文档、测试、配置模板**（137 个文件，约 1.7 MB）
- 不含 git 历史（`.git` 未复制），副本是纯文件目录

## 二、已排除内容（未随副本分发）

| 类别 | 具体内容 | 原因 |
|------|----------|------|
| 密钥与配置 | `.env`、`admin/.env` | 含真实 API Key、升级对象 ID（已改为 `.env.example` / `admin/.env.example`） |
| 数据库 | `data/`（`tickets.db` 及全部备份、`groups.json` 群配置、`.admin_password`） | 真实工单、真实群与人员 ID、管理密码 |
| 日志 | `logs/` | 含真实消息内容 |
| 归档图片 | `archives/attachments/` | 真实钉钉群聊天图片 |
| 依赖与大文件 | `.venv/`、`bin/cloudflared`（38 MB）、`__pycache__/`、`.pytest_cache/` | 体积，可重建 |
| 线下产出 | `框架/`（含真实门店名与工单正文） | 真实业务内容 |
| 凭证目录 | `dws-data/`、`dws-keychain/` | 钉钉 CLI 登录凭证 |

## 三、脱敏规则（全局一致替换）

替换是**组件级、全局一致**的：同一实体在代码、文档、测试中一律替换为同一占位符，因此测试断言与实现两侧同步改写，语义与可运行性不变。

| 类别 | 占位形式 | 示例形态 |
|------|----------|----------|
| 人员姓名 | 角色 + 字母 | `工程总监A`、`工程师A`~`工程师E`、`设计主管A`、`店长A`~`店长F`、`区域经理A/B` |
| openDingtalkId | `oid_<角色>_<字母>` | `oid_engineer_a`、`oid_manager_a`、`oid_listener_bot` |
| 数字 userId | `9` 开头等长假值 | 18/17/16 位分别映射为 `900000000000000001` 等同长度值 |
| 群 cid | `cid_demo_group_<字母>` | `cid_demo_group_a` … `cid_demo_group_g` |
| AI 表格 baseId | `base_demo_0001` | — |
| 真实淘宝订单号 | `9990000000000000001` 起 | 保持 19 位形态 |
| 门店名 | 保留城市，替换商场与品牌 | `上海商场02店`、`上海品牌A商场01店` |
| 商场/位置词 | `商场01` … `商场25` | 各具体商场名逐一映射，映射关系一一对应 |
| 自有品牌词 | `品牌A`~`品牌D` | 含品牌英文名与中文名两种写法 |
| 测试群名 | `测试群（示例）` | — |
| 本机路径 | `/Users/example` | 原为本机用户名 |
| 组织/仓库标识 | `example-org` | — |
| 注释中的示例密钥 | `<redacted>` | `.env.example` 内的历史注释值 |

## 四、使用方式

```bash
cp .env.example .env            # 填入自己的模型/视觉服务密钥
cp admin/.env.example admin/.env
python3 -m venv .venv && .venv/bin/pip install -r requirements.txt
python3 -m pytest -q            # 运行测试
python main.py --test           # 测试群影子模式（需自备钉钉 CLI 与群配置）
```

`data/groups.json`（群与人员配置）未随副本分发，需按 `config.py` 中的结构自行准备：

```json
{"groups": [{"group_id": "...", "store_name": "...", "manager_ids": [], "engineer_ids": [],
             "engineering_leader_id": "", "regional_manager_id": "", "is_active": true}],
 "user_id_map": {"<openDingtalkId>": "<userId>"}}
```

## 五、注意

- 本副本**不含任何真实姓名、账号 ID、群 ID、门店名、订单号与密钥**；占位符与原值的对照表未随副本分发。
- 占位符是**虚构值**，请勿用于任何真实环境；实际部署时按你的组织重新配置。

## 六、附带内容

- `data/groups.example.json`：**由真实群配置按同一套脱敏规则生成**（ID、门店名、群号全部为上述占位值，无真实数据），用于让副本可启动/可测试。
  用法：`cp data/groups.example.json data/groups.json`，或临时 `GROUPS_CONFIG_PATH=data/groups.example.json python3 -m pytest -q`。
- `脱敏说明.md`（本文件）。

## 七、验证记录

- 副本全部 `.py` 通过语法编译；全部 `.json` 可解析。
- 残留扫描：真实姓名、openDingtalkId、数字 userId、群 cid、门店名、商场/品牌名、真实订单号、本机用户路径、API Key 均**无残留**（`sk-` 命中的 3 处为原仓库自带的假密钥占位串，非真实密钥）。
- 以 `data/groups.example.json` 运行完整测试套件：**410 passed / 1 failed**。
  唯一失败用例 `tests/test_pipeline_integration.py::test_select_context_then_route_by_context`，
  原因是门店名占位符 `测试群（示例）` 含全角括号，而门店名会参与工单编号解析，括号干扰了解析。
  **按需求方要求不做功能性修复**——本次交付目标为信息脱敏，副本不承诺开箱即跑通全部测试。
  如需该用例通过，把占位符改为不含标点的形式（如 `示例测试群`）并全局一致替换即可。
